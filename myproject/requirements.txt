# Django 5.2.6
Django>=5.2,<6.0

# REST Framework
djangorestframework>=3.14,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-filter>=23.5,<25.0

# PDF Generation
reportlab>=4.0,<5.0

# Database
psycopg2-binary>=2.9,<3.0
# Parses DATABASE_URL="postgres://user:pass@host:5432/db" into Django DATABASES dict
# Used in settings.py to switch between SQLite (dev) and PostgreSQL (prod)
dj-database-url>=2.1.0,<3.0

# CORS
django-cors-headers>=4.3,<5.0

# Production server
gunicorn>=21.2,<22.0

# Testing
pytest>=7.4,<9.0
pytest-django>=4.7,<5.0
pytest-cov>=4.1,<6.0

# AWS Storage & Secrets
# django-storages: allows Django to store files in S3 instead of local disk
# boto3: AWS SDK (reads IAM role credentials automatically in ECS — no keys needed!)
django-storages[s3]>=1.14.0,<2.0
boto3>=1.34.0,<2.0
